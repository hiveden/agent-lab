# Data Plane 本地开发变量
# 复制为 .env（不进 git）
# 生产环境在 Fly.io 用 fly secrets set 设置

# 环境标识 — 设为 production 触发启动校验
DEPLOY_ENV=development

# LLM
LLM_MOCK=1
LLM_PROVIDER=glm
GLM_API_KEY=
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
LLM_MODEL_PUSH=glm-4-flash
LLM_MODEL_CHAT=glm-4.6

# Platform API
RADAR_WRITE_TOKEN=dev-radar-token-change-me
PLATFORM_API_BASE=http://127.0.0.1:8788
DEFAULT_USER_ID=default_user

# Agent 服务
RADAR_AGENT_PORT=8001

# 网络代理（本地 ClashX，Fly.io 不需要）
HTTPS_PROXY=http://127.0.0.1:7890
HTTP_PROXY=http://127.0.0.1:7890
NO_PROXY=127.0.0.1,localhost
